apiVersion: v1
kind: Service
metadata:
  name: dhl-dashboard
  labels:
    app: dhl-dashboard
spec:
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
  selector:
    app: dhl-dashboard
